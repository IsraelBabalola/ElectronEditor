{
  "name": "electron-image-editor",
  "version": "1.0.0",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently \"npm --prefix client run dev\" \"npm run electron\"",
    "electron": "wait-on http://localhost:5173 && electron ."
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "wait-on": "^7.0.1",
    "electron": "^28.0.0"
  }
}
